<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MeetingTranscriber.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MeetingTranscriber.App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="MeetingTranscriber.App">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Comprobaciones de empaquetado" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock
                Text="Andamio temporal. Se borra cuando exista MeetingTranscriber.Cli, que es donde van los diagnosticos."
                Style="{StaticResource CaptionTextBlockStyle}"
                Opacity="0.7" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
            <Button x:Name="EnvironmentButton" Click="OnRunEnvironmentProbe" Content="1 · Entorno del proceso hijo" />
            <Button x:Name="FileSystemButton" Click="OnRunFileSystemProbe" Content="2 · Ruta de escritura" />
            <Button x:Name="SaveButton" Click="OnSaveReport" Content="Guardar informe" />
        </StackPanel>

        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto">
            <TextBlock
                x:Name="OutputText"
                FontFamily="Cascadia Mono, Consolas"
                FontSize="12"
                IsTextSelectionEnabled="True"
                TextWrapping="NoWrap" />
        </ScrollViewer>

        <TextBlock
            Grid.Row="3"
            x:Name="StatusText"
            Style="{StaticResource CaptionTextBlockStyle}"
            Opacity="0.7" />
    </Grid>
</Window>
